package main

// The PyPI mirror provider and connectivity probe were moved into the
// internal/core/pip package and are bootstrapped from server.Run (after the
// database is initialized). Their unit tests live in that package.
